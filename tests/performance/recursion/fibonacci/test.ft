def fibonacci(i32 num) -> i32:
	if num <= 1:
		return num;
	else:
		i32 fib_n_1 = fibonacci(num - 1);
		i32 fib_n_2 = fibonacci(num - 2);
		return fib_n_1 + fib_n_2;

def main():
	i32 fib = fibonacci(40);
	print(fib);
