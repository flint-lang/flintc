# fsh

The fsh example is a simple interactive shell, written in Flint. Fsh stands for **F**lint **Sh**ell. There is no `Core.system` core module available at the moment, so the print calls actually do nothing at the moment. But it teaches the basics of string manipulation and parsing.
