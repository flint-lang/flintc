use Core.print
use Core.parse

def sum(u32 n) -> u64:
	u64 total = 0;
	for (i, _) in 0..(n + 1):
		total += i;
	return total;

def main(str[] args):
	u32 n = 1_000_000;
	if args.len > 1:
		n = parse_u32(args[1]);
	print($"{sum(n)}\n");
