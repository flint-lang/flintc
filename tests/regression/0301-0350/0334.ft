use Core.print

def main():
	i32[] arr = i32[10](0);
	for (idx, elem) in arr:
		elem = i32(idx);

	print($"arr.length = {arr.length}\n");
	i32[] slice = arr[2..8];
	print($"slice.length = {slice.length}\n");
