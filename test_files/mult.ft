use "add.ft"

def mult(i32 x, i32 y) -> i32:
	return x * y;

// --- THE TESTS ---
use "assert.ft"

test "4 * 5 = 20":
	i32 res = mult(4, 5);
	assert(res == 20);

test "4 + 5 * 2 = 14":
	i32 res = mult(5, 2);
	assert(4 + res == 14);