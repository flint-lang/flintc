//use "library.ft";
//use flint.math.vectors;

def main():
	int fib = fibonnaci(40);
	print(fib);

def fibonnaci(int num) -> int:
	if num <= 1:
		return num;
	else:
		int fib_n_1 = fibonnaci(num - 1);
		int fib_n_2 = fibonnaci(num - 2);
		return fib_n_1 + fib_n_2;
