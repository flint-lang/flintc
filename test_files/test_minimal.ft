use Core.print

data Data:
	i32 x;
	i32 y;
	Data(x, y);

interface Interface:
	def print();

func Func requires(Data d):
	def print():
		print($"d.(x, y) = {d.(x, y)}\n");

object Object:
	data: Data;
	func: Func;
	Object(Data);

def main():
	o := Object(Data(10, 20));
	o.print();
