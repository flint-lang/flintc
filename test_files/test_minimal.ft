use Core.print
use Core.math
use Core.assert
use Core.system

use "imported.ft"

data MyData:
	i32 x;
	f32 y;
	MyData(x, y);

def some_function(i32 x, i32 y) -> i32 {ErrAssert}:
	if x == 0:
		throw ErrAssert.AssertionFailed;
	return x + y;

error MyError:
	V1, V2, V3;

def main():
	i32 x = 121;
	i32 y = 234;
	print($"max({x}, {y}) = {max(x, y)}\n");

	print($"z = {x}\n");
	some_function(10, 20);
	func1(10, 20);

	some_function(0, 1);
