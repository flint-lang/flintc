use Core.print

def get_strings() -> (str, str):
	return ("Hi", "there");

def print_and_add(i32 x, i32 y) -> i32:
	print($"adding {x} and {y}\n");
	return x + y;

def main():
	(_, y) := get_strings();
	print($"y is \"{y}\"\n");

	_ = print_and_add(30, 40);
