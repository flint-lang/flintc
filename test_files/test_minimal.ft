use Core.print

def main():
    i32[,] arr = i32[5, 6](0);
    arr[3, 3] = 5;
    i32[] slice = arr[3, 2..4];
    print($"slice.len = {slice.len}\n");
    for (i, elem) in slice:
        print($"slice[i] = {elem}\n");
