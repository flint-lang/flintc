use Core.print

data Data:
	i32 x;
	i32 y;
	Data(x, y);

def some_function(mut Data d, const str s):
	d.x = 40;
	print($"d.(x, y) = {d.(x, y)}\n");
	print($"s = {s}\n");

def main():
	d := Data(10, 20);
	s := "Hi, There";
	some_function(d, s);
