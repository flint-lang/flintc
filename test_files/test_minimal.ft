use "assert.ft"

def test_mult_fn(i32x4 v4, i32 m) -> i32x4:
    return v4 * m;

def main():
    i32x4 inn = (1, 2, 3, 4);
    i32x4 out = test_mult_fn(inn, 3);
    assert(out == (3, 6, 9, 12));
    print($"in = ({inn.r}, {inn.g}, {inn.b}, {inn.a})\n");
