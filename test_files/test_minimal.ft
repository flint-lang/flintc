use Core.print

data Data:
	u64 x = 0;
	u64 y = 1;
	u64 z = 2;
	Data(x, y, z);

def main():
	Data[] d = Data[5](Data(_));

	for (i, elem) in d:
		print($"d[{i}] = Data(.x = {elem.x}, .y = {elem.y}, .z = {elem.z}\n");
