def main():
    str a = "A";
    a += "B";
    print(a);
    i64 b = -16;
    print($"{b}\n");
    // i64[] arr;
    // arr = i64[10];
    // arr[0] = 5;
    // arr[1] = arr[0] + 1;
    // arr[2] = arr[1] + arr[0]; 
