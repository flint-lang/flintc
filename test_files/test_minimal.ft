use Core.print as p

data Data1:
	i32 x;
	i32 y;
	Data1(x, y);

entity Entity:
	data: Data1 d1;
	Entity(d1);

	def print():
		p.print($"d1.(x, y) = {d1.(x, y)}\n");

def main():
	e := Entity(Data1(10, 20));
	e.print();
