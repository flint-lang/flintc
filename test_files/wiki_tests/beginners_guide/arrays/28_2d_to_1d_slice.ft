use Core.print

def main():
	i32[,] arr = i32[10, 10](0);
	(I, J) := arr.length;
	for j := 0; j < J; j++:
		for i := 0; i < I; i++:
			arr[i, j] = (i + 1) * (j + 1);

	i32[] slice = arr[1, 2..8];
	for (idx, elem) in slice:
		print($"slice[{idx}] = {elem}\n");
