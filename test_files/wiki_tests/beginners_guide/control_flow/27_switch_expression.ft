use Core.print

enum MyEnum:
    VAL1, VAL2, VAL3;

def main():
    MyEnum e = MyEnum.VAL1;
    i32 result = switch e:
        VAL1 -> 1;
        VAL2 -> 2;
        VAL3 -> 4;
    print($"result = {result}\n");
