use Core.print

data Vec2:
	i32 x;
	i32 y;
	Vec2(x, y);

def inc(mut Vec2? vec):
	if vec == none:
		return;
	vec!.(x, y) += (1, 1);

def main():
	Vec2 v2 = Vec2(10, 20);
	Vec2? v2m = v2;
	inc(v2m);
	
	(x, y) := v2m!.(x, y);
	print($"(x, y) = ({x}, {y})\n");
