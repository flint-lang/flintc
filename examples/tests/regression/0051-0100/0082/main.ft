use Core.print

extern def foo(mut i32 x, mut i32 y) -> i32;
extern def bar() -> i32;

def main():
	i32 res = foo(10, 20);
	print($"res = {res}\n");
	res = bar();
	print($"res = {res}\n");
	print("Hello, World!\n");
