use Core.print
use Fip.c

def main():
	c := Container(null, 0);
	print($"c.value = {c.value}\n");
	print($"c.len = {c.len}\n");

	con := create();
	if con.value == null:
		print("c.value is null\n");
	else:
		print("c.value points to something\n");
	print($"c.len = {con.len}\n");
	destroy(&con);
