use Core.print

data MyData:
	i32 x;
	f32 y;
	str v;
	MyData(x, y, v);

def main():
	MyData? ref = none;
	if true:
		MyData val = MyData(10, 3.14, "segfault");
		ref = val;
	print($"ref.x = {ref!.x}\n");
