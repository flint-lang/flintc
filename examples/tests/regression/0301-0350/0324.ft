use Core.print
use Core.time

def main():
	TimeStamp begin = now();
	TimeUnit tu = TimeUnit.US;
	switch tu:
		NS: print("ns\n");
		US: print("µs\n");
		MS: print("ms\n");
		S:  print("s\n");
	TimeStamp middle = now();
	Duration duration = duration(begin, middle);
	print($"duration = {duration.value}\n");
	duration.value = duration.value * 100;
	sleep(duration);
	TimeStamp end = now();
	duration = duration(middle, end);
	print($"duration = {duration.value}\n");
