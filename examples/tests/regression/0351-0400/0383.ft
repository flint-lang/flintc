use Core.print

//extern def create() -> Container;
//extern def destroy(mut Container* c);

data Container:
	opaque value;
	u64 len;
	Container(value, len);

def main():
	c := Container(null, 0);
	print($"c.len = {c.len}\n");
	//print($"c.value = {c.value}\n");
	//c := create();
	//print($"c.len = {c.len}\n");
	//destroy(&c);
