use Core.print

def main():
	i32[,] arr = i32[5, 5](1);
	for (i, elem) in arr:
		elem = i32(i);
	i32[] arr2 = arr[1, 1..];
	for (i, elem) in arr2:
		if i > 0:
			print(", ");
		print(elem);
	print("\n");
