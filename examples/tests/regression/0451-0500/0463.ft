use Core.print

data Point:
	i32 x;
	i32 y;
	i32 z;
	Point(x, y, z);

def create_point(i32 x, i32 y, i32 z) -> Point:
	return Point(x, y, z);

def main():
    Point p = create_point(5, 7, 9);
    print($"Point(x: {p.x}, y: {p.y}, z: {p.z})\n");
