use Core.print

def main():
	i32[,] arr = i32[10, 10](0);
	(I, J) := arr.length;
	for u32 j = 0; j < J; j++:
		for u32 i = 0; i < I; i++:
			arr[i, j] = i32((i + 1) * (j + 1));

	i32[,] slice = arr[3..6, 2..8];
	for (idx, elem) in slice:
		print($"slice[{idx}] = {elem}\n");
